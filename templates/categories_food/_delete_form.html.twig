<form method="post" action="{{ path('categories_food_delete', {'id': categories_food.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categories_food.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
